name: album
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  sqflite: ^2.0.0+4
  path_provider: ^2.0.11
  cached_network_image: ^3.2.1


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons cass for iOS style icons.
  cupertino_icons: ^1.0.8
  get_it: ^8.0.2
  meta: ^1.15.0
  dartz: ^0.10.1
  http: ^1.2.2
  path: ^1.9.0
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mocktail: ^1.0.4
  bloc_test: ^9.1.7


flutter:
  uses-material-design: true
